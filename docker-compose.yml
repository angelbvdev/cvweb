services:
  cvweb:
    build: .
    container_name: cvweb
    env_file:
      - .env
    ports:
      - "5000:5000"
    volumes:
      - cvweb_data:/data
      - cvweb_uploads:/app/app/static/uploads
    restart: unless-stopped

volumes:
  cvweb_data:
  cvweb_uploads:
